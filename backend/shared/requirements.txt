# Dependencies for the shared Lambda layer.
# These are installed into the layer and available to all Lambda functions.
# Kept in sync with backend/requirements.txt so the FastAPI app (served via
# Mangum from the API Lambdas) has all runtime dependencies.
fastapi==0.115.0
mangum==0.17.0
pydantic>=2.9.0,<3.0.0
pydantic-settings>=2.5.2,<3.0.0
boto3>=1.37.0,<2.0.0
python-multipart==0.0.9
python-dateutil==2.9.0
strands-agents==0.3.0
starlette>=0.37.2,<0.39.0
