/**
 * API service layer - Axios instance with auth interceptor.
 *
 * Endpoints:
 * - uploadInvoice()
 * - getInvoices()
 * - getInvoiceDetail(id)
 * - approveInvoice(id, action, comment)
 * - uploadDocument()
 * - getDocuments()
 * - syncKnowledgeBase()
 * - askQuestion(question, sessionId?, category?)
 * - getChatSessions()
 * - getSessionHistory(sessionId)
 * - getDashboardStats()
 */
