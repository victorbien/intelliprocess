/**
 * Shared TypeScript types and interfaces.
 *
 * Types to define:
 * - Invoice, InvoiceStatus, ExtractionResult, ConfidenceField
 * - MatchResult, POMatchResult, GRMatchResult
 * - ChatMessage, Citation, ChatResponse
 * - DashboardStats, ActivityItem
 * - User, UserRole
 * - ApiResponse<T>, PaginatedResponse<T>
 */
